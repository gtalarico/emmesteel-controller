{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "sync homeassistant component",
            "type": "shell",
            "command": "rsync -avz --rsync-path=\"sudo rsync\"  ./home-assistant homeassistant.local:/config/custom_components/emmesteel"
        }
    ]
}
